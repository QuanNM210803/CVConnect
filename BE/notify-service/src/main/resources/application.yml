spring:
  application:
    name: notify-service
  datasource:
    url: jdbc:h2:mem:notify-service
    username: root
    password: sadjfoasdhfnaoga
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  h2:
    console:
      enabled: false
      path: /h2-console
  data:
    mongodb:
      host: ${DB_HOST:localhost}
      port: 27017
      database: cvconnect-notify-service
      username: root
      password: root
      authentication-database: admin
  kafka:
    producer:
      bootstrap-servers: ${KAFKA_HOST:localhost}:${KAFKA_PORT:9094}
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      bootstrap-servers: ${KAFKA_HOST:localhost}:${KAFKA_PORT:9094}
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      group-id: notify-service
  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: ${MAIL_USERNAME:784652002@smtp-brevo.com}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    sender: nnmhqn2003@gmail.com

server:
  port: 8081
  port-socket: 9090
  servlet:
    context-path: /notify

logging:
  level:
    org:
      springframework:
        security: info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

jwt:
  expiration: 86400 # 1 day in seconds
  secret-key: TaqlmGv1iEDMRiFp/pHuID1+T84IABfuA0xXh4GhiUI=
  refresh-expiration: 604800 # 7 days in seconds
  x-internal-token: k118wSpPDIVURys

frontend:
  url-candidate: http://localhost:3000
  url-management-member: http://localhost:3000
  url-organization-member: http://localhost:3000

open-api:
  url-gateway-localhost: http://localhost:8888/api/v1${server.servlet.context-path}
  url-gateway-radmin: http://26.9.175.39:8888/api/v1${server.servlet.context-path}
  url-localhost: http://localhost:${server.port}${server.servlet.context-path}
  url-radmin: http://26.9.175.39:${server.port}${server.servlet.context-path}

redis:
  token-key: token